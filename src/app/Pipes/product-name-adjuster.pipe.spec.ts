import { ProductNameAdjusterPipe } from './product-name-adjuster.pipe';

describe('ProductNameAdjusterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductNameAdjusterPipe();
    expect(pipe).toBeTruthy();
  });
});
